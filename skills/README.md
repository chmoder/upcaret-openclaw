# Skills

Add one directory per skill. Folder name should match how you want it referenced locally (e.g. `advisor-lead-gen`).

Each skill must include at minimum:

- `SKILL.md` with YAML frontmatter (`name`, `description`)
- Any scripts and prompts the skill references

Do not commit runtime databases (e.g. `advisors.db`) or API keys.
